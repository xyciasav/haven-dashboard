services:
  haven:
    image: haven-dashboard:0.2.9
    build:
      context: .
      args:
        HAVEN_VERSION: 0.2.9
    container_name: haven
    restart: unless-stopped
    ports:
      - "43127:3000"
    environment:
      HAVEN_USER_NAME: Mike
      # Choose a long random value. It unlocks authentication settings in Haven.
      HAVEN_SETUP_TOKEN: ${HAVEN_SETUP_TOKEN:-}
      HAVEN_AUTH_BYPASS: ${HAVEN_AUTH_BYPASS:-false}
      KEYCLOAK_ENABLED: "false"
      KEYCLOAK_URL: ""
      KEYCLOAK_REALM: ""
      KEYCLOAK_CLIENT_ID: haven
      # Set these in Portainer. Never commit the real token.
      HOME_ASSISTANT_URL: ${HOME_ASSISTANT_URL:-}
      HOME_ASSISTANT_TOKEN: ${HOME_ASSISTANT_TOKEN:-}
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp
    volumes:
      - haven-data:/app/data
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:3000/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s

volumes:
  haven-data:
